to store image in storage folder, use the following command:
php artisan storage:link

install quill ide
npm install quill@2.0.3
npm run build

in your resources/app.js
import Quill from "quill";
window.Quill = Quill;

npm install animate.css --save
import 'animate.css'; in resources/app.css

to use local email for testing. we will use mailhog
install mailhog from https://github.com/mailhog/MailHog
